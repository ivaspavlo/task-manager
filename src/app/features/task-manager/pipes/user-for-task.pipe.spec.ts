import { UserForTaskPipe } from './user-for-task.pipe';

describe('UserForTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new UserForTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
