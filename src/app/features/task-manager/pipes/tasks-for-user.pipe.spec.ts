import { TasksForUserPipe } from './tasks-for-user.pipe';

describe('TasksForUserPipe', () => {
  it('create an instance', () => {
    const pipe = new TasksForUserPipe();
    expect(pipe).toBeTruthy();
  });
});
